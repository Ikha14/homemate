---
phase: 01-bootstrap-substrat
plan: 03
plan_type: execute
wave: 3
depends_on: ["01-02"]
files_modified: ["bin/fm-wake-lib.sh", "bin/fm-watch.sh", "bin/fm-supervise-daemon.sh", "bin/fm-teardown.sh", "bin/fm-spawn.sh"]
autonomous: true
description: "Lock mkdir (gel wake queue), pgid portable, sleeps interruptibles"
type: Plan
about: "homemate"
---

<objective>
## Goal
Le point de rupture n°1 (lock symlink → gel infini de la wake queue sous MSYS) disparaît : verrou répertoire `mkdir` atomique gaté MSYS ; le pgid des checks vient du wrapper ; les boucles chaudes deviennent interruptibles.

## Purpose
Sans ça, `fm_wake_append` hang à l'infini (prouvé ticket 05) — aucune supervision possible.

## Output
fm-wake-lib.sh (3 primitives + mode dir), fm-watch.sh (pgid L483, sleeps), fm-supervise-daemon.sh (fallback kill -0, sleeps), fm-teardown.sh + fm-spawn.sh (sleeps ≥1 s).
</objective>

<context>
Constat de lecture : la couche amont est DÉJÀ à moitié dir-aware (`fm_lock_release` branche else lit `$lockdir/pid` + rmdir ; `fm_lock_recheck_stale_owner` accepte `[ -d ] && [ ! -L ]` ; `fm_lock_try_acquire` lit `$lockdir/pid` sans readlink). Seules `fm_lock_try_create`, `fm_lock_points_to_owner`, `fm_lock_link_owner` sont symlink-only → on n'ajoute qu'une branche dir gatée `FM_LOCK_DIR_MODE` (=`FM_PS_MSYS`, fm-ps-lib déjà sourcée par wake-lib depuis 01-02). POSIX : byte-identique.
Ticket 05 : kill de groupe MARCHE sous `set -m` (leader + petit-enfant tués) — seul l'approvisionnement pgid (`ps -o pgid=`) casse → fm_ps_field pgid (procfs stat champ 5). Daemon : boucle watchdog `kill -0 "-$pid"` → fallback single-pid sinon watchdog sauté et `wait` bloquant.
Sleeps : remplacement `sleep N & wait $!` limité aux sleeps ≥1 s des boucles à trap (audit) ; les 0.1-0.3 s restent nus (fork MSYS ~5 ms, latence déjà ≤ durée).
</context>

<acceptance_criteria>
## AC-1: Wake queue vivante sous contention
Given fm-wake-lib sourcée sous Git Bash (state temporaire)
When 3 racers × N `fm_wake_append` concurrents (timeout global)
Then zéro hang, total lignes = 3×N, zéro entrelacement, locks libérés (pas de `.lock` résiduel)

## AC-2: Stale claim
Given un lock répertoire dont le pid est mort et vieux
When `fm_lock_try_acquire`
Then le lock est volé proprement (steal), acquis, pid = mien

## AC-3: pgid + fallback kill
Given fm-watch.sh / fm-supervise-daemon.sh
Then L483 lit le pgid via fm_ps_field ; la boucle watchdog du daemon teste groupe PUIS pid simple ; `bash -n` verts

## AC-4: Sleeps interruptibles
Given les sleeps ≥1 s listés (daemon 1415/1436/1446/1461, watch 583/599/616/639, teardown 545/593, spawn 852)
Then pattern `& wait $! || true` en place, `bash -n` verts
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Mode dir des 3 primitives lock</name>
  <files>bin/fm-wake-lib.sh</files>
  <action>FM_LOCK_DIR_MODE=$FM_PS_MSYS après le sourcing fm-ps-lib. Branches dir : try_create = mkdir atomique + pid dedans + garde steal ; points_to_owner = égalité chemin + [ -d ] && [ ! -L ] ; link_owner = renvoie lockdir si dir. Rien d'autre ne bouge.</action>
  <verify>AC-1 + AC-2 en scratch</verify>
  <done>AC-1, AC-2</done>
</task>
<task type="auto">
  <name>Task 2: pgid watch + fallback daemon</name>
  <files>bin/fm-watch.sh, bin/fm-supervise-daemon.sh</files>
  <action>watch L483 → fm_ps_field pgid (sourcer fm-ps-lib selon pattern du fichier) ; daemon L697 → `kill -0 "-$pid" || kill -0 "$pid"`.</action>
  <verify>bash -n ; grep</verify>
  <done>AC-3</done>
</task>
<task type="auto">
  <name>Task 3: Sleeps ≥1 s interruptibles</name>
  <files>bin/fm-supervise-daemon.sh, bin/fm-watch.sh, bin/fm-teardown.sh, bin/fm-spawn.sh</files>
  <action>`sleep X` → `sleep X & wait $! || true` aux lignes listées. Les 0.1-0.3 s restent nus (choix documenté).</action>
  <verify>bash -n ×4 ; trap TERM réagit < 0,1 s sur un mini-scénario scratch</verify>
  <done>AC-4</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Machine d'états lock (acquire_wait/try_acquire/claim/steal) — seules les 3 primitives gagnent une branche
- Comportement POSIX (gate stricte)
- Périphérie exclue
## SCOPE LIMITS
- Écarts amont (2)/(4) : constatés/testés ici, pas de refonte au-delà du mode dir
- Pas de commit avant gate 01-04
</boundaries>

<verification>
- [ ] AC-1..4
- [ ] bash -n sur les 5 fichiers
</verification>

<success_criteria>
- fm_wake_append ne peut plus geler sous MSYS
- Kill de groupe alimenté et dégradable
</success_criteria>

<output>
01-03-SUMMARY.md
</output>
