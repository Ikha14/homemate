---
phase: 01-bootstrap-substrat
plan: 01
plan_type: execute
wave: 1
depends_on: []
files_modified: [".gitattributes", "bin/fm-ensure-agents-md.sh", "bin/fm-win-copies.sh"]
autonomous: true
description: "Checkout sûr (.gitattributes) + symlinks committés remplacés par copies régénérées"
type: Plan
about: "homemate"
---

<objective>
## Goal
Le clone survit à un re-clone autocrlf et les deux symlinks committés (`CLAUDE.md`→AGENTS.md, `.claude/skills`→`.agents/skills`) deviennent des copies régénérées fonctionnelles sous Windows, sans salir l'arbre git.

## Purpose
US 13 de la spec : Claude Code doit lire les vraies instructions. Sans ça, toute session Claude Code dans le clone lit un fichier de 9 octets (« AGENTS.md », cible du symlink dégradé).

## Output
- `.gitattributes` (`*.sh text eol=lf`)
- `bin/fm-ensure-agents-md.sh` : branche MSYS copie-régénérée (POSIX intact)
- `bin/fm-win-copies.sh` (nouveau) : régénère les copies du clone lui-même + `skip-worktree`
</objective>

<context>
## Project Context
@.paul/PROJECT.md (celui du CLONE — pas celui de la racine OneDrive)

## Source Files
@bin/fm-ensure-agents-md.sh
@"# Obsidian_Second_Memory/projects/outils/homemate/audit-windowsismes-bin.md" (lignes 97, 102-106, 158, 188, 197)

## Décisions spec applicables
- `core.symlinks=true` PROSCRIT. Copies régénérées, jamais committées. AGENTS.md = source unique.
- Détection plateforme : `uname -s` contient MSYS/MINGW → branche Windows. Ailleurs : comportement amont inchangé (minimal-invasif).
- Décision plan (non couverte par la spec, tranchée ici) : les copies régénérées du clone masquées par `git update-index --skip-worktree` — l'arbre reste propre, rien n'est jamais committé.
</context>

<acceptance_criteria>
## AC-1: Checkout sûr
Given le dépôt avec `.gitattributes` posé
When `git add .gitattributes && git status`
Then aucun `*.sh` n'apparaît modifié (checkout déjà LF) et un futur re-clone autocrlf checkouterait les `.sh` en LF

## AC-2: fm-ensure-agents-md.sh portable
Given un répertoire de test hors clone avec un AGENTS.md réel et pas de CLAUDE.md, sous Git Bash MSYS
When `bin/fm-ensure-agents-md.sh <dir>` tourne deux fois
Then run 1 : CLAUDE.md créé comme FICHIER RÉEL au contenu identique à AGENTS.md ; run 2 : sortie « unchanged », zéro modification ; et si AGENTS.md change, un run suivant régénère la copie

## AC-3: Copies du clone régénérées, arbre propre
Given le clone avec ses deux symlinks dégradés (CLAUDE.md 9 octets, .claude/skills fichier texte)
When `bin/fm-win-copies.sh` tourne
Then CLAUDE.md = copie réelle d'AGENTS.md ; `.claude/skills/` = vrai répertoire miroir de `.agents/skills/` ; `git status --short` vide ; script idempotent (2e run = no-op)
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Poser .gitattributes</name>
  <files>.gitattributes</files>
  <action>
    Créer `.gitattributes` à la racine : `*.sh text eol=lf` (+ `*.bash text eol=lf` si présent dans le repo, sinon s'abstenir — diff minimal).
    Vérifier qu'aucune renormalisation ne s'imprime (checkout 100 % LF déjà).
  </action>
  <verify>git add .gitattributes && git status --short → seul .gitattributes stagé, aucun .sh modifié</verify>
  <done>AC-1 satisfaite</done>
</task>

<task type="auto">
  <name>Task 2: Brancher fm-ensure-agents-md.sh en mode copie sous MSYS</name>
  <files>bin/fm-ensure-agents-md.sh</files>
  <action>
    Ajouter une détection `case "$(uname -s)" in MSYS*|MINGW*)` → mode copie :
    - L158/188/197 : `ln -s "$AGENTS" "$CLAUDE"` → copie réelle (`cp`) + marqueur d'origine (comparaison de contenu au lieu de readlink)
    - `is_correct_claude_symlink` (L95-111) : sous MSYS, « correct » = CLAUDE.md fichier réel au contenu identique à AGENTS.md (cmp -s) ; si différent parce qu'AGENTS.md a évolué → régénérer, pas conflit. Attention branche L166-168 (« both real files » = conflit) : sous MSYS ce cas devient le cas NOMINAL — ne conflit que si CLAUDE.md diverge d'AGENTS.md ET n'est pas régénérable (heuristique : si CLAUDE.md n'est pas un sous-ensemble/copie d'une version d'AGENTS.md, préserver le conflit pour ne pas écraser du contenu humain ; simple et sûr : conflit seulement si les DEUX diffèrent ET CLAUDE.md non vide non identique — garder le message conflict amont).
    - L102 : fallback `command -v python3 || command -v python` (audit L48).
    - Hors MSYS : ZÉRO changement de comportement (minimal-invasif).
  </action>
  <verify>Répertoire de test scratchpad : les 3 scénarios d'AC-2 + scénario « promoted » (CLAUDE.md réel seul → move + copie) passent sous Git Bash</verify>
  <done>AC-2 satisfaite</done>
</task>

<task type="auto">
  <name>Task 3: fm-win-copies.sh — régénérer les copies du clone</name>
  <files>bin/fm-win-copies.sh</files>
  <action>
    Nouveau script (style maison : `set -eu`, usage, shebang env bash) :
    1. Se placer à la racine du repo (`git rev-parse --show-toplevel`).
    2. CLAUDE.md : si contenu = « AGENTS.md » (symlink dégradé) ou copie obsolète → remplacer par copie d'AGENTS.md ; `git update-index --skip-worktree CLAUDE.md`.
    3. `.claude/skills` : si fichier texte dégradé → supprimer, copier `.agents/skills/` en vrai répertoire ; `git update-index --skip-worktree .claude/skills` (le blob symlink tracké).
    4. Idempotent : re-run sans changement = no-op silencieux.
    Ne PAS toucher aux autres fichiers ; ne rien committer.
  </action>
  <verify>bash bin/fm-win-copies.sh && head -3 CLAUDE.md (contenu AGENTS.md réel) && ls .claude/skills/ (répertoire) && git status --short (vide) ; re-run = aucune sortie d'erreur, état stable</verify>
  <done>AC-3 satisfaite</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Comportement POSIX/macOS de fm-ensure-agents-md.sh (branches Darwin/Linux intactes)
- Noms internes `fm-*`, AGENTS.md comme source unique
- Périphérie hors scope (X mode, autres backends)

## SCOPE LIMITS
- Pas de portage ps/lock/sleep ici (plans 01-02/01-03)
- Pas de commit tant que le lot phase 1 n'a pas passé /code-review
- Pas d'adaptation des tests amont ici (plan 01-04)
</boundaries>

<verification>
- [ ] Les 3 AC vérifiées par commandes
- [ ] `bash -n` sur les deux scripts (parse OK)
- [ ] `git status --short` vide après fm-win-copies.sh
</verification>

<success_criteria>
- Claude Code lancé dans le clone lirait le vrai contenu AGENTS.md via CLAUDE.md
- Arbre git propre, rien de committé
- Zéro régression du chemin POSIX
</success_criteria>

<output>
After completion, create `.paul/phases/01-bootstrap-substrat/01-01-SUMMARY.md`
</output>
