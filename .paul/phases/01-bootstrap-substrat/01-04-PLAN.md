---
phase: 01-bootstrap-substrat
plan: 04
plan_type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03"]
files_modified: ["tests/fm-win-substrate.test.sh"]
autonomous: true
description: "Tests substrat Windows + tests amont phase 1 + gate /code-review + commit lot"
type: Plan
about: "homemate"
---

<objective>
## Goal
Le socle phase 1 est prouvé par des tests committés, la suite amont pertinente passe (ou ses casses sont expliquées Windows), le lot passe la gate /code-review et part en commit local.
</objective>

<context>
Scripts ticket-05 originaux perdus (scratchpad session précédente : seuls ticket-04/ et ticket-07/ subsistent) → le test de substrat est réécrit dans le dépôt à partir des scénarios prouvés (contention mkdir via vraie fm_wake_append, steal stale, interruptibilité sleep&wait). Style tests/ amont (voir tests/lib.sh).
</context>

<acceptance_criteria>
## AC-1: tests/fm-win-substrate.test.sh vert sous Git Bash (skip propre hors MSYS)
## AC-2: fm-ensure-agents-md.test.sh + fm-wake-queue.test.sh + fm-watcher-lock.test.sh exécutés ; toute casse classée (régression à corriger vs assert POSIX à adapter)
## AC-3: /code-review du diff = GO (findings bloquants corrigés) ; commit local du lot (pas de push)
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Test substrat committé</name>
  <files>tests/fm-win-substrate.test.sh</files>
  <action>3 sections : contention wake queue (3×10, total exact, 5 champs, zéro lock résiduel, timeout global), steal stale, réactivité trap sur sleep&wait (<0,5 s). Skip explicite hors MSYS.</action>
  <verify>bash tests/fm-win-substrate.test.sh → PASS</verify>
  <done>AC-1</done>
</task>
<task type="auto">
  <name>Task 2: Suite amont phase 1</name>
  <files>(lecture seule)</files>
  <action>Lancer les 3 tests amont sous Git Bash ; trier les échecs ; adapter les scripts portés si régression réelle.</action>
  <verify>Sortie des runs + classement</verify>
  <done>AC-2</done>
</task>
<task type="auto">
  <name>Task 3: Gate + commit</name>
  <files>(diff du lot)</files>
  <action>/code-review sur le diff ; corriger les findings bloquants ; commit local message court français.</action>
  <verify>Verdict review + git log</verify>
  <done>AC-3</done>
</task>
</tasks>

<boundaries>
## SCOPE LIMITS
- Pas de push sans demande d'Ilian
- Adaptations de tests amont limitées aux asserts plateformes (jamais affaiblir un invariant)
</boundaries>

<verification>
- [ ] AC-1..3
</verification>

<success_criteria>
- Lot phase 1 committé, gate passée
</success_criteria>

<output>
01-04-SUMMARY.md
</output>
